# Used only if you switch the Railway builder from DOCKERFILE to NIXPACKS.
providers = ["node"]

[phases.setup]
nixPkgs = ["nodejs_20"]

[phases.build]
cmds = ["npm install", "npm run build"]

[start]
cmd = "npm start"
